<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Googlemap" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
         <item name="colorPrimary">@color/slinks</item>
    </style>

    <style name="Theme.Googlemap" parent="Base.Theme.Googlemap" />
</resources>